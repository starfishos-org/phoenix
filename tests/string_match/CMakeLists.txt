set(TEST_NAME string_match)
if(${OS} STREQUAL "CHCORE")
    set(TEST_NAME ${TEST_NAME}.bin)
endif()
add_executable(${TEST_NAME} string_match.c)
target_link_libraries(${TEST_NAME} PRIVATE ${LIB_NAME})
